# 项目协作规范

## 修改范围

- 只处理当前任务，保留工作区和暂存区中已有的改动；未经要求不提交、推送或修改相邻仓库。
- 优先复用 MeowUI、EzHookTool 和项目已有封装。依赖版本以 `gradle/libs.versions.toml` 为准，不擅自调整工具链。
- `../MeowUI` 通过复合构建接入。修改调用方式前核对其公开 API；库内规则见该仓库的 `AGENTS.md`。

## 模块规范

各目录的 `AGENTS.md` 补充本文件，修改前阅读对应模块的规则。

| 模块 | 职责与规范 |
| --- | --- |
| [app](app/AGENTS.md) | 应用入口、框架服务桥接、日志存储、Manifest 与打包 |
| [ui](ui/AGENTS.md) | Compose 页面、MeowUI 组件、导航、外观与本地化 |
| [shared](shared/AGENTS.md) | 设置契约、跨进程协议、设备判断与共用工具 |
| [hook](hook/AGENTS.md) | 注入入口、功能生命周期、宿主适配、缓存与日志 |
| [hidden-api](hidden-api/AGENTS.md) | 平台隐藏类的编译桩与打包隔离 |

依赖保持 `app → ui / hook → shared`；不得反向引用应用实现。隐藏 API 桩只通过编译期依赖提供。

## 跨模块修改

- 新增设置从 `shared/Preferences.kt` 开始，明确键名、类型、默认值、单位和生效方式，再接 UI 与 Hook。
- 新增宿主同步核对 `Scope`、`scope.list`、Hook 注册和首页宿主表；需要新日志标题时更新 UI 映射。
- 修改持久化数据、组件名或协议字段时考虑已有配置；不把枚举序号作为稳定标识。
- 调用框架或宿主 API 前核对当前依赖及目标版本，设备差异用有依据的分支处理。

## 注释

- 默认不加注释；名称和结构已能说明的内容不再复述。
- 仅解释不明显的原因、兼容性限制、协议约定，以及线程、生命周期或安全边界。
- 注释紧贴相关代码，尽量一两句说清楚。不写开发流水账、聊天口吻、废弃方案或未经验证的性能结论。
- 修改逻辑时同步检查注释，删除过时内容；保留许可证和有用的来源说明。
- 长期协作规则放在本文件，任务进度放在 `.codex-context/`，不塞进源码。

## 验证与交付

- 先明确完成标准，再做与改动相称的检查。只改注释时验证可执行内容未变，无需编译。
- 编译、安装、设备重启和耗时测试遵循用户当前授权；暂停的任务不自行恢复。
- 不暂存或提交 `.codex-context/`、本机配置、密钥和构建产物。
- 用简明中文说明实际改动和验证结果，区分静态检查、编译通过与实机验证。
