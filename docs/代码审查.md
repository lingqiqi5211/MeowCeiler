## 2026-07-31 18:02

## 代码审查报告

### 审查目标

- 类型：本地改动
- 范围：HyperCeiler 的 #1660、onPackageReady 与热重载改动；EzHookTool 1.1.3 的 API 101/102 能力选择与生命周期改动
- 审查时间：2026-07-31 18:02

### 审查输入证据

- Diff 范围：HyperCeiler 当前工作树中的热重载相关文件；EzHookTool 当前 7 个修改文件
- 自动化检查：EzHookTool `:core:test`、API 82/102 release assemble、全模块 Maven Local 发布通过；HyperCeiler `:app:assembleDebug` 通过
- 真机检查：安装后 system_server、SystemUI、Home 与壁纸进程号不变；24 个进程接受热重载，21 个完成；API 102 误判为 0
- 其他上下文：用户明确选择 HyperCeiler 保持真实 `onPackageReady`，EzHookTool early 入口仅作为手动选择能力保留

### 问题清单

#### 🔴 严重问题

无。

#### 🟡 建议修改

无待处理项。EzHookTool 一处遗漏 early 入口的 KDoc 已在审查中修正；Maven Local 已补齐 API 82 模块的 1.1.3。

#### 🟢 可选优化

- SystemUI 等启用资源替换的进程会按设计拒绝热重载，应继续通过目标进程重启验证，不应计为 EzHookTool 故障。
- MiShare 新功能与 AGP 9.3.1 升级不属于本次提交范围，后续应独立审阅与验证。

### 亮点

- API 101 路径不反射 Xposed API，API 102 专属类型被隔离并由 consumer rule 保留。
- API 102 能力按运行时版本选择，手动开关和 early 生命周期入口均保留。
- HyperCeiler 初次加载与热重载统一使用 `PackageTarget`，恢复顺序与 ClassLoader 语义一致。
- 真机自动热重载已覆盖 system_server、Home、壁纸、相机、设置与镜像等长生命周期目标。

### 结论

- 结论：`Approved`
- 严重问题：0 个
- 建议修改：0 个
- 可选优化：2 个
- 下一步建议：发布 EzHookTool 1.1.3，确认 Maven Central 三个模块可用后，再提交并推送 HyperCeiler 相关改动。

---
